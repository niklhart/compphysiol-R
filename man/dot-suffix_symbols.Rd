% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expression-handling.R
\name{.suffix_symbols}
\alias{.suffix_symbols}
\title{Recursively append a suffix to variable symbols in an expression}
\usage{
.suffix_symbols(expr, suffix, skip = character())
}
\arguments{
\item{expr}{A quoted R expression (e.g., from \code{quote()}).}

\item{suffix}{A character string to append to variable names.}

\item{skip}{A character vector of symbol names that should not be suffixed.}
}
\value{
A new expression with the suffix appended to variable symbols.
}
\description{
This function traverses the abstract syntax tree (AST) of a quoted R
expression and appends a suffix to all variable symbols, \emph{except}:
\itemize{
\item function names in calls (e.g. \code{f(x)} keeps \code{f} unchanged),
\item symbols listed in \code{skip}.
}
}
\examples{
expr <- quote(a * b + f(c, g(d)))
.suffix_symbols(expr, "_s")
# Returns: quote(a_s * b_s + f(c_s, g(d_s)))

# Skip specific variables
.suffix_symbols(expr, "_s", skip = c("c", "d"))
# Returns: quote(a_s * b_s + f(c, g(d)))

}
