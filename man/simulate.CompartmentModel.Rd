% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Simulation.R
\name{simulate.CompartmentModel}
\alias{simulate.CompartmentModel}
\title{Simulate a compartment model}
\usage{
\method{simulate}{CompartmentModel}(
  object,
  nsim = NULL,
  seed = NULL,
  time = numeric(0),
  unit = NULL,
  parameters = list(),
  dimensions = NULL,
  simulation_type = c("ode", "analytical", "ssa", "hybrid"),
  partition = NULL,
  include_event_times = FALSE,
  ...
)
}
\arguments{
\item{object}{A \code{CompartmentModel} object.}

\item{nsim}{Number of stochastic realizations for \code{simulation_type = "ssa"}.
Ignored by deterministic simulation routes.}

\item{seed}{Optional random seed for stochastic simulation routes.}

\item{time}{Simulation time points. Units can be supplied with the unit
shorthand, e.g. \code{seq(0, 24) [h]}.}

\item{unit}{Optional time unit used when \code{time} is numeric without units.}

\item{parameters}{Free parameters passed to the ODE solver, as a named list
or \code{Parameters} object.}

\item{dimensions}{Named list of unit dimensions defining the numerical scale
used at the solver boundary. These dimensions affect the scale of solver
tolerances such as \code{atol}.}

\item{simulation_type}{Simulation route. \code{"ode"} uses the established
numerical ODE path. \code{"analytical"} uses matrix exponentials for supported
linear models. \code{"ssa"} uses Gillespie's stochastic simulation algorithm.
\code{"hybrid"} uses the Alfonsi hybrid stochastic-deterministic algorithm.}

\item{partition}{Partitioning for \code{simulation_type = "hybrid"}. A logical
vector marks stochastic reactions with \code{TRUE} and deterministic reactions
with \code{FALSE}. A non-negative numeric scalar enables adaptive partitioning:
reactions with propensities below this threshold are stochastic.}

\item{include_event_times}{Include stochastic event times in stochastic
simulation output in addition to the requested \code{time} points.}

\item{...}{Additional arguments passed to \code{\link[deSolve:ode]{deSolve::ode()}}.}
}
\value{
A \code{SimulationResult} object.
}
\description{
\code{simulate()} solves a \code{CompartmentModel} and returns toolbox-level output.
The default route wraps the \code{deSolve} ODE export and returns ODE states and
observables.
}
\details{
Simulation is either unit-free or unit-aware with respect to time. If the
model uses a time dimension, \code{time} must carry units through the unit
shorthand or the \code{unit} argument. Conversely, unit-aware simulation times
require a model with a time dimension.

In unit-aware simulations, \code{dimensions} defines the solver-facing numerical
scale used when unit-bearing model quantities are converted to plain numbers
before calling \code{\link[deSolve:ode]{deSolve::ode()}}. This scale also determines how solver
tolerances such as \code{atol} are interpreted. For example, if amounts are
converted to kilograms internally, an \code{atol} value is applied on the kilogram
scale even if results are displayed back in milligrams.
}
\examples{
M <- multiCompModel(ncomp = 1, type = "micro", unit = "mg") |>
    add_dosing(time = 0 [h], amount = 100 [mg], cmt = "cen") |>
    wire(what = "molec")

out <- simulate(
    M,
    time = seq(0, 24, by = 1) [h],
    parameters = parameters(kc0 = 0.2 [1/h], Vcen = 10 [L])
)
out$states
out$observables
}
